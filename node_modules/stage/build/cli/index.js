"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = __importDefault(require("commander"));
(async () => {
    const program = new commander_1.default.Command();
    // Add commands
    program
        .command("run")
        .description("Run the app locally.")
        .action(async () => require("./commands/run").runCommand());
    program
        .command("build")
        .description("Build app assets.")
        .action(async () => require("./commands/build").buildCommand());
    program
        .command("deploy")
        .description("Deploy to your AWS account.")
        .action(async () => require("./commands/deploy").deployCommand());
    // Default command: run
    if (process.argv.length < 3) {
        process.argv.splice(2, 0, "run");
    }
    program.parse(process.argv);
})();
